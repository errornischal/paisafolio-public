# Secrets and local environment files never belong in git.
.env
.env.*
!.env.example
*.pem
*.key
.vercel/
node_modules/
.DS_Store
*.log

# sb-config.js is tracked on purpose: its two values are public by design and a fork
# fills them in. The service_role key is an environment variable, never a file here.
